//We use this to avoid typos inside the reducer and action files, this modularized the code
const CartActionTypes = {
    TOGGLE_CART_HIDDEN: 'TOGGLE_CART_HIDDEN',
    ADD_ITEM: 'ADD_ITEM'
};

export default CartActionTypes;